# Local and hosted builds should use the runtime configuration provided by the
# environment. Do not force SQLx offline mode here; it can break hosted builds
# when the cached query metadata drifts from the actual schema.
